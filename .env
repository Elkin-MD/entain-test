# Local (non-docker) defaults. Under docker compose these are overridden by
# the values in docker-compose.yml (DB_HOST=db).
HTTP_PORT=8080
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASS=postgres
DB_NAME=entaintest
DB_SSLMODE=disable
